<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runner.core.mapper.CodeMapper">

    <select id="selectAllCodeGroups" resultType="com.runner.core.domain.CodeGroup">
        SELECT
            group_code as groupCode,
            group_name as groupName,
            description,
            use_yn as useYn,
            created_at as createdAt,
            updated_at as updatedAt
        FROM CODE_GROUP
        WHERE use_yn = 'Y'
        ORDER BY group_code ASC
    </select>

    <select id="selectCodeGroupByCode" parameterType="String" resultType="com.runner.core.domain.CodeGroup">
        SELECT
            group_code as groupCode,
            group_name as groupName,
            description,
            use_yn as useYn,
            created_at as createdAt,
            updated_at as updatedAt
        FROM CODE_GROUP
        WHERE group_code = #{groupCode}
    </select>

</mapper>