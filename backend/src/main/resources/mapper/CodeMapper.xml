<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runner.core.code.mapper.CodeMapper">

    <sql id="codeGroupColumns">
        group_code as groupCode,
        group_name as groupName,
        description,
        use_yn as useYn,
        created_at as createdAt,
        updated_at as updatedAt
    </sql>

    <select id="findAllCodeGroups" resultType="CodeGroup">
        SELECT
            <include refid="codeGroupColumns"/>
        FROM CODE_GROUP
        WHERE use_yn = 'Y'
        ORDER BY group_code ASC
    </select>

    <select id="findCodeGroupByGroupCode" parameterType="String" resultType="CodeGroup">
        SELECT
            <include refid="codeGroupColumns"/>
        FROM CODE_GROUP
        WHERE group_code = #{groupCode}
    </select>

    <select id="findCodeDetailsByGroupCode" parameterType="String" resultType="CodeDetail">
        SELECT
            code_id as codeId,
            group_code as groupCode,
            code_value as codeValue,
            code_name as codeName,
            description,
            sort_order as sortOrder,
            use_yn as useYn,
            created_at as createdAt,
            updated_at as updatedAt
        FROM CODE_DETAIL
        WHERE group_code = #{groupCode} AND use_yn = 'Y'
        ORDER BY sort_order ASC
    </select>

</mapper>